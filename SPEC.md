# Excel Dashboard Builder - 기능 명세서

## 개요
엑셀 파일(.xlsx, .xls, .csv)을 업로드하면 즉시 시각화 대시보드를 자동 생성하는 **HTML 단일 파일** 웹 애플리케이션.
서버 없이 브라우저에서 바로 동작하며, 데이터는 외부로 전송되지 않음.

## 파일 구조
```
c:\workspace\ppt\
├── dashboard.html          ← 원본 (개발/수정용)
├── dashboard.dist.html     ← 배포용 (난독화+소스보호)
├── dashboard.ico           ← 바탕화면 바로가기 아이콘
├── build.js                ← 빌드 스크립트 (node build.js → dist 생성)
├── create_icon.py          ← 아이콘+바로가기 생성 스크립트
├── SPEC.md                 ← 이 문서
├── app.py                  ← (미사용) 초기 Flask 서버 버전
├── requirements.txt        ← (미사용) Flask 의존성
├── create_sample.py        ← (미사용) 샘플 데이터 생성
├── sample_sales.xlsx       ← (미사용) 테스트용 샘플
├── templates/index.html    ← (미사용) Flask 프론트엔드
└── node_modules/           ← build.js 의존성
```

## 기술 스택
- **프론트엔드**: 순수 HTML/CSS/JS (단일 파일)
- **폰트**: Pretendard (CDN)
- **차트**: ECharts 5.5.1 (CDN)
- **엑셀 파싱**: SheetJS (xlsx) 0.18.5 (CDN)
- **이미지 캡처**: html2canvas 1.4.1 (CDN)
- **난독화**: javascript-obfuscator (빌드 시)
- **인터넷 필수**: CDN 라이브러리 로드를 위해

## 빌드/배포
```bash
# 원본 수정 후 배포용 생성
cd c:/workspace/ppt
node build.js

# 바탕화면 바로가기 생성
python create_icon.py
```
- dashboard.html 수정 시 **반드시** `node build.js` 실행하여 dashboard.dist.html 동기화
- 판매 시 dashboard.dist.html 만 전달

---

## 기능 상세

### 1. 엑셀 업로드
- **드래그 & 드롭** 또는 클릭으로 파일 선택
- 지원 형식: `.xlsx`, `.xls`, `.csv`
- 브라우저에서 직접 파싱 (서버 전송 없음)
- 업로드 영역: 화면 세로 중앙 정렬, 배경에 차트 아이콘 패턴 (업로드 박스 바깥에 표시)

### 2. 스마트 엑셀 파싱 (`smartParseSheet`)
일반적인 엑셀 파일의 다양한 구조를 자동 처리:
- **타이틀 행 건너뛰기**: 상위 10행 내에서 실제 헤더 행 자동 감지 (비어있지 않은 셀이 가장 많은 문자열 행)
- **2줄 병합 헤더 결합**: "연차휴가" + "발생일수" → "연차휴가/발생일수"
- **빈 행 건너뛰기**: 데이터 없는 행 자동 제외
- **빈 컬럼 제거**: 90% 이상 비어있는 컬럼 자동 삭제
- **끝쪽 빈 헤더 제거**: 엑셀 끝 불필요 빈 컬럼 정리
- **중복 헤더명 처리**: 같은 이름이면 `_2`, `_3` 접미사
- **날짜 처리**: `cellDates: true` + `raw: false`로 날짜 셀 자동 변환
- **엑셀 날짜 시리얼 변환**: 46082 같은 숫자 → "2026-03-18" 형식

### 3. 데이터 분석 (`parseAndInit`)
컬럼 타입을 자동 분류:
- **날짜 컬럼 감지** (`isDateLike`): "날짜","일자","date" 등 키워드, 엑셀 시리얼(30000~60000), YYYY-MM-DD 패턴
- **YYYYMM 형식 재분류**: `202505` 같은 숫자를 텍스트로 재분류
- **숫자형 vs 텍스트형**: 70% 이상 숫자인 컬럼 → 숫자형
- **금액 컬럼 감지**: 이름에 "금액","단가","합계","매출","계약" 등 포함 → 우선 선택
- **카테고리 컬럼 감지**: 유니크 값 2~20개 → 차트 X축으로 적합
- **ID 컬럼 구분**: 유니크 값이 많으면 차트 X축에서 제외

### 4. 자동 위젯 생성
데이터 분석 결과에 따라 자동으로 위젯 생성:
1. **요약 카드**: 금액 컬럼 합계/평균 표시 (금액 키워드 컬럼 우선)
2. **막대 차트**: 카테고리 컬럼 × 금액 컬럼 (그룹별 합계)
3. **라인 차트**: 카테고리 × 금액 2~3개 (추세)
4. **파이 차트**: 카테고리형 컬럼 값 분포
5. **도넛 차트**: 두 번째 카테고리 컬럼 (있으면)
6. **데이터 미리보기**: 리스트 위젯 (상위 20행)

### 5. 위젯 시스템

#### 5-1. 위젯 유형 (8종)
| 유형 | 설명 |
|------|------|
| **카드** | 숫자 컬럼의 합계/평균을 카드형으로 표시. flex-wrap으로 공간에 맞게 한 줄/여러 줄 자동 조절. 금액은 white-space:nowrap으로 줄바꿈 방지 |
| **리스트** | 선택한 컬럼을 테이블로 표시. 행 수 조절 가능(10/20/30/50). 헤더와 짧은 텍스트는 nowrap, 긴 텍스트(평균 10자 초과)만 줄바꿈 허용. 가로 스크롤 자동 생성 |
| **막대 차트** | X축/Y축 컬럼 선택. 카테고리형이면 그룹별 합계 자동 집계 |
| **라인 차트** | smooth 곡선, 그라디언트 area fill |
| **영역 차트** | 라인 + 강한 그라디언트 area fill |
| **파이** | 값 분포. 8개 초과 시 "기타"로 묶음. 1위 80% 초과 시 가로 막대로 자동 전환 |
| **도넛** | 파이의 도넛 변형 (동일 로직) |
| **산점도** | 숫자 컬럼 2개의 상관관계 |

#### 5-2. 위젯 관리 팝업
- 상단 "위젯 관리" 버튼 클릭 → 모달 열림
- 현재 위젯 목록이 행으로 표시됨
- 각 행: 제목 input + 유형 select + 삭제 버튼
- 유형 변경 시 해당 유형에 맞는 X축/Y축/컬럼 선택 필드 자동 표시
- "저장" 버튼으로 대시보드에 반영

#### 5-3. 위젯 조작
- **드래그 앤 드롭**: 핸들(점 6개 아이콘)을 잡고 위치 교환
- **크기 변경**: 3단계 토글 (1칸 → 2칸 → 전체 → 1칸). 확대 시 dataZoom 숨기고 전체 데이터 표시
- **타이틀 편집**: 텍스트 또는 연필 아이콘 클릭 → 인라인 input → Enter/blur로 저장, Esc로 취소
- **삭제**: X 버튼

#### 5-4. 위젯 UI
- 크기 변경/삭제 버튼에 커스텀 툴팁 (위쪽 말풍선, data-tip 속성)
- dataZoom이 있는 차트에 "확장하면 전체 보기 →" 힌트 (확대 시 자동 숨김, 깜빡이는 애니메이션)
- 차트 위에 데이터 정보 표시: `X: 영업구분  Y: 총계약금액 (그룹별 합계)`
- 리스트 행 hover/click 시 테마 primary 색상 배경(10% 알파) + primary-light 텍스트

### 6. 차트 엔진 (ECharts)

#### 6-1. 스타일
- 네온 글로우 + 그라디언트 (다크 배경 최적화)
- 막대: 상단 둥근 모서리, 상→하 그라디언트, shadowBlur 글로우
- 라인: smooth 곡선, 선 자체 shadowBlur, 그라디언트 area fill
- 파이/도넛: borderRadius, 호버 시 확대+글로우
- 툴팁: 다크 배경(card-bg), 보더(border-color)

#### 6-2. 데이터 집계
- X축이 카테고리형(유니크 값 < 행수 50% & ≤50개)이면 **그룹별 합계** 자동 집계
- 그 외(ID/날짜 등 유니크 많음)에는 행 단위 표시 (상위 50행)
- 파이/도넛: 건수(count) 집계, 상위 8개 + "기타"

#### 6-3. 대량 데이터 처리
- 15개 초과 라벨 시 하단 dataZoom 슬라이더 자동 추가
- 위젯 확대(size-large/full) 시 dataZoom 숨기고 전체 표시
- X축 라벨 자동 회전(8개 초과 시 35도), 자동 간격

### 7. 테마 시스템
상단 우측 원형 도트 5개로 즉시 전환:

| 테마 | Primary | 차트 팔레트 톤 |
|------|---------|-------------|
| Midnight | #00A15D (그린) | 네온 그린/시안/퍼플 |
| Purple | #8B5CF6 | 퍼플/핑크/라벤더 |
| Ocean | #06B6D4 (시안) | 시안/블루/민트 |
| Rose | #F43F5E | 핑크/로즈/레드 |
| Ember | #F97316 (오렌지) | 오렌지/옐로우/앰버 |

- 테마 변경 시: 로고, 버튼, 차트 팔레트, 카드 accent 색상 변경
- 레이아웃 색상은 **고정**: body `#161717`, card `#202020`, border `#2B2B2B`, text `#A8ADB7`, title `#fff`

### 8. 이미지 다운로드
- 상단 "저장" 버튼 클릭
- html2canvas로 대시보드 전체를 PNG 캡처
- **헤더/위젯관리바 제외** (캡처 시 숨김 후 복원)
- **파일명이 타이틀로** 표시 (캡처 직전 임시 div 삽입 후 제거)
- 다운로드 파일명: `엑셀파일명_2026-03-30.png`
- 배경색: 현재 테마의 --body-bg 동적 적용

### 9. UI/UX 디자인

#### 9-1. 디자인 시스템 (Workload 다크모드 기반)
- Body: `#161717`, Card: `#202020`, Border: `#2B2B2B`
- Text: `#A8ADB7`, Title: `#fff`, Muted: `#828690`
- border-radius: `1.25rem`
- box-shadow: 없음 (Workload 다크모드와 동일)

#### 9-2. 폰트
- Pretendard (CDN)
- Body: 15px, 헤더 타이틀: 18px/700, 섹션: 18px/700
- 카드 숫자: 20px/800, 차트 제목: 15px/700

#### 9-3. 스크롤바
- 6px 얇은 스크롤바, 반투명 (`rgba(255,255,255,.12)`)
- hover 시 밝아짐 (`rgba(255,255,255,.22)`)

#### 9-4. 반응형
- 3컬럼 (기본) → 2컬럼 (1200px 이하) → 1컬럼 (768px 이하)
- 업로드 영역: 90% 너비, max 800px, 세로 중앙 정렬

### 10. 소스 보호 (배포용)
`node build.js` 실행 시 적용:
- JS 난독화: 변수/함수명 16진수 변환, 문자열 Base64 암호화, 제어흐름 평탄화, 데드코드 삽입
- CSS 압축 (주석/공백 제거)
- HTML 압축
- 우클릭 차단
- F12 / Ctrl+Shift+I / Ctrl+U 단축키 차단
- 개발자도구 감지 시 화면 차단 메시지
- 콘솔 출력 비활성화
- 디버거 보호 (debugger 문 주기 실행)

### 11. 바탕화면 바로가기
- `python create_icon.py` 실행
- 4배 크기로 그린 후 LANCZOS 리샘플링 (고해상도)
- 초록 둥근 사각형 + 흰색 막대차트 3개 아이콘
- 크기: 16/24/32/48/64/128/256px ICO
- 원본용 + 배포용 2개 바로가기 생성

---

## 프로젝트 진행 현황 (2026-04-12)

### ✅ 1단계 완료 — 기존 페이지 SEO/콘텐츠 보강

Dashboard Builder (`_src_index.html`) 및 QR/바코드 (`qr-generator.html`) 하단에 다음 섹션 추가:

| 섹션 | Dashboard Builder | QR/바코드 |
|------|:-----------------:|:---------:|
| 서비스 소개 (300자+) | ✅ | ✅ |
| 사용 사례 6개 (카드형) | ✅ | ✅ |
| 단계별 사용법 (3~4단계) | ✅ | ✅ |
| 주요 기능 목록 | ✅ | ✅ |
| 지원 형식·제한 안내 | ✅ | ✅ |
| FAQ (8개/6개, `<details>` 아코디언) | ✅ | ✅ |
| 관련 도구 링크 | ✅ | ✅ |

각 페이지 2,500자 이상 텍스트 확보. AdSense 크롤러용 실질적 콘텐츠.

### 🔜 2단계 예정 — POP 메이커 (신규 페이지)

- 파일명: `pop-maker.html`
- 서비스명: 미정 (사장님 POP 메이커 / 매장 안내문 만들기 / POP 메이커 중 선택)
- 첫 템플릿 3종: 신메뉴 출시 / 휴무 안내 / 할인 이벤트
- 구성: 도구 섹션 + 설명 섹션 (SEO)

---

## 알려진 제한사항
- 인터넷 연결 필수 (CDN 라이브러리)
- 매우 큰 파일(수만 행)은 브라우저 메모리 제한
- 엑셀 매크로/수식은 무시 (값만 읽음)
- 멀티 시트는 첫 번째 시트만 처리
