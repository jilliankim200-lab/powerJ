# 날씨 패션 추천 시스템 기획서

## 개요

Dashboard Builder 앱의 세 번째 탭으로 추가.  
접속자 위치 기반 날씨를 분석해 캐릭터가 오늘의 옷차림을 추천해주는 페이지.

---

## 탭 위치

```
[ Dashboard Builder ]  [ QR / 바코드 ]  [ 오늘 뭐 입지? ]  ← NEW
```

---

## 핵심 플로우

```
접속
 ↓
위치 권한 요청 (Geolocation API)
 ↓
Open-Meteo API 호출 (키 불필요)
https://api.open-meteo.com/v1/forecast
  ?latitude={lat}&longitude={lon}
  &current=temperature_2m,weathercode,precipitation
 ↓
날씨 조건 분석
 ↓
캐릭터 이미지 + 추천 문구 애니메이션 등장 (짜잔~)
 ↓
이미지 다운로드 버튼
```

---

## 날씨 조건 → 아이템 매핑

| 조건 | 기온 기준 | 추천 아이템 | 캐릭터 이미지 |
|------|----------|------------|--------------|
| 눈 / 영하 | ~ -1°C | 롱패딩, 머플러, 장갑, 부츠 | `weather_snow.png` |
| 매우 추움 | 0 ~ 4°C | 패딩, 목도리, 니트 | `weather_cold.png` |
| 추움 | 5 ~ 11°C | 코트, 후드, 레이어드 | `weather_cool.png` |
| 선선 | 12 ~ 16°C | 자켓, 가디건, 청바지 | `weather_mild.png` |
| 포근 | 17 ~ 22°C | 긴팔 티, 슬랙스 | `weather_warm.png` |
| 더움 | 23°C ~ | 반팔, 반바지, 선글라스 | `weather_hot.png` |
| 비 | (기온 무관) | 우산, 레인코트, 방수화 | `weather_rain.png` |
| 흐림 | (기온 무관) | 가디건, 얇은 아우터 | `weather_cloudy.png` |

> 비 조건은 기온 조건보다 우선 적용

---

## 추천 문구 예시

| 조건 | 문구 |
|------|------|
| 눈 | "❄️ 눈이 와요! 따뜻하게 챙겨 입어요~" |
| 매우 추움 | "🧊 오늘 많이 춥네요. 두꺼운 패딩 필수!" |
| 추움 | "🧥 쌀쌀해요. 코트 꺼내세요!" |
| 선선 | "🍂 자켓 하나면 딱이에요~" |
| 포근 | "🌤 오늘 날씨 완벽해요! 긴팔이면 충분~" |
| 더움 | "☀️ 더워요! 반팔 + 선크림 챙기세요" |
| 비 | "🌧 비 와요. 우산 꼭 챙기세요!" |
| 흐림 | "☁️ 흐린 날. 겉옷 하나 챙겨요~" |

---

## UI 구성

```
┌─────────────────────────────────────────────────┐
│  📍 서울특별시 · 13°C · 흐림                      │
├─────────────────────────────────────────────────┤
│                                                  │
│          [캐릭터 이미지 - 짜잔 애니메이션]          │
│                                                  │
│       🧥 자켓 하나면 딱이에요~                     │
│                                                  │
│    ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│    │  자켓   │  │ 가디건  │  │ 청바지  │        │
│    └─────────┘  └─────────┘  └─────────┘        │
│                                                  │
│          [ 📥 이미지 저장하기 ]                    │
└─────────────────────────────────────────────────┘
```

---

## 애니메이션 연출

1. **페이지 진입** → 로딩 스피너 (위치/날씨 조회 중)
2. **데이터 수신** → 배경 색상 전환 (날씨별 컬러)
3. **캐릭터 등장** → 아래에서 bounce-in 효과 ("짜잔~" 효과음 옵션)
4. **아이템 태그** → 캐릭터 주변에 말풍선/태그 형태로 순차 등장
5. **다운로드** → html2canvas로 현재 카드 캡쳐 → PNG 저장

---

## 날씨별 배경 컬러

| 조건 | 배경 그라디언트 |
|------|--------------|
| 맑음/더움 | `#FFD93D → #FF6B35` |
| 포근 | `#74C7EC → #A8E6CF` |
| 선선/흐림 | `#B0BEC5 → #78909C` |
| 추움/매우추움 | `#90CAF9 → #5C6BC0` |
| 눈 | `#E3F2FD → #90CAF9` |
| 비 | `#546E7A → #37474F` |

---

## 기술 스택

| 항목 | 내용 |
|------|------|
| 위치 | `navigator.geolocation.getCurrentPosition()` |
| 날씨 API | Open-Meteo (무료, 키 불필요) |
| 역지오코딩 | `nominatim.openstreetmap.org` (무료, 키 불필요) |
| 이미지 다운로드 | `html2canvas` (이미 프로젝트에 포함됨) |
| 애니메이션 | CSS keyframes (bounce, fadeIn) |
| 이미지 경로 | `/images/weather_*.png` |

---

## 필요한 캐릭터 이미지 목록

준비 필요한 파일 8종:

```
/c/workspace/ppt/docs/images/
├── weather_snow.png      # 영하/눈 — 롱패딩 착용
├── weather_cold.png      # 0~4°C — 패딩 착용
├── weather_cool.png      # 5~11°C — 코트 착용
├── weather_mild.png      # 12~16°C — 자켓 착용
├── weather_warm.png      # 17~22°C — 긴팔 착용
├── weather_hot.png       # 23°C~ — 반팔 착용
├── weather_rain.png      # 비 — 우산/레인코트
└── weather_cloudy.png    # 흐림 — 얇은 아우터
```

---

## 개발 순서

1. `docs/` 폴더에 `weather.html` 생성
2. 기존 `index.html` 헤더에 탭 추가 (난독화 전 `_src_index.html` 수정)
3. 캐릭터 이미지 준비 후 `/docs/images/` 에 배치
4. 빌드 (`node build.js`) 후 배포
